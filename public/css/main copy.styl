@import url('https://fonts.googleapis.com/css?family=Roboto:300,400,300italic,400italic&display=swap');

font-text = 'Roboto';
color-text = #212121;
color-card = #E7E6E1;
color-card-dark-mode = #222831;
color-card-inner-container-dark-mode =  #33313b;
color-card-outer-container-dark-mode = #222831;
color-text-dark-mode = #ececec;
color-step = #F57C00;
color-bg = #FF9800;
color-bg-dark-mode = #f2a365;

// Title
h1 {
    margin-bottom: 2.5em;
}

h2 {
    margin: 2em;
}

// Anchor
a {
    color: color-step;
}

a:hover {
    color: color-bg;
}

// create a class shared by all pseudo elements depicting a step of the flow (each altering only the content property)
.tooltip {
    // position the tooltip in the top right of the container
    position: absolute;
    right: 0;
    bottom: 100%;
    // style the tooltip with the chosen color values
    color: color-card;
    background: color-step;
    text-transform: uppercase;
    font-size: 0.9rem;
    padding: 0.25rem 0.75rem;
    border-radius: 2.5px;
}

// create a class shared by all cards
.card {
    line-height: 2;
    background: color-card;
    padding: 1.2rem 1rem;
    border-radius: 4px;
    box-shadow: 0 2px 10px darken(color-card, 10%);
}

* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

body {
    min-height: 100vh;
    width: 100%;
    font-family: font-text;
    color: color-text;
    background: color-bg;
}

// separate the main container from the surrounding page
.container {
    margin: 5vh 2.5vw;
    padding: 15vh 0;
    background: color-card;
    border-radius: 5px;
    // display the content in a single column layout
    display: flex;
    flex-direction: column;
    // center the content
    align-items: center;
    text-align: center;

    // for the SVG separating the containers, set a reasonable height
    svg {
        height: 5rem;

        line {
            stroke: color-bg;
            stroke-width: 3px;
            stroke-linecap: round;
            // include a stroke-dasharray depicting small dots separated by considerable space
            stroke-dasharray: 2px 20px;
            // animate the stroke-dashoffset property to move the dotted line downward
            animation: animateline 5s linear both infinite;
        }
    }
}

// style all headings and paragraphs with the same values
h3 {
    font-size: 1.1rem;
    color: darken(color-text, 25%);
}

p {
    font-size: 0.95rem;
    font-weight: 300;
}

.container__validation {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Validation';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__startup-coaching {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Startup Coaching';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__teambuilding {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Team Building';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__promotion {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Promotion';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__cloud {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Cloud';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__container {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Container';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__website {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Website';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__design {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Design';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__payments {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Payments';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__management {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Management';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__email {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'email';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__remote-work {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Remote Work';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__incorporation {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Incorporation';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__investment {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Investment';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__finance {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Finance';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__marketing {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Marketing';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__legal {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Legal';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__analytics {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Analytics';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__operations {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Operations';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__datascience {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Data Science';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__manufacturing {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Manufacturing';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__software-development {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Software Development';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__equity {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Equity';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__3D_Printing {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: '3D Printing';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

.container__Monitoring {
    // display the contents of the first container in a single row
    display: flex;
    border-radius: 8px;
    padding: 1.5rem;
    background: darken(color-card, 2.5%);
    // position relative for the pseudo element
    position: relative;

    &:before {
        @extend .tooltip;
        content: 'Monitoring';
    }

    div {
        // beside the properties shared by all cards, move the text to the left and separate the containers horizontally
        @extend .card;
        text-align: left;
        margin: 0 1rem;
    }
}

footer {
    padding: 0 0 1.5em 1.5em;
}

footer a {
    color: #212121;
}

footer a:hover {
    color: #FFE0B2;
}



// on smaller viewports lay the three cards on top of one another
@media (max-width: 1160px) {
    .container__validation, .container__startup-coaching, .container__teambuilding, .container__promotion, .container__cloud, .container__container, .container__website, .container__design, .container__payments, .container__management, .container__investment, .container__incorporation, .container__email, .container__remote-work, .container__finance, .container__marketing, .container__legal, .container__analytics, .container__operations, .container__datascience, .container__manufacturing, .container__software-development, .container__equity, .container__3D_Printing, .container__Monitoring {
        flex-direction: column;

        div {
            margin: 1rem 0;
        }
    }
}

// Dark Mode
@media (prefers-color-scheme: dark) {
    body{
        background: color-bg-dark-mode;
    }
    h1, h2, h3, h4, h5, p, li {
    color: color-text-dark-mode;
    }
    .tooltip {
        color: color-text;
        background: color-bg-dark-mode;
    }  
    .card{
        background: color-card-dark-mode;
    }
   .container {
        background: color-card-outer-container-dark-mode;
    }
    .container__validation, .container__startup-coaching, .container__teambuilding, .container__promotion, .container__cloud, .container__container, .container__website, .container__design, .container__payments, .container__management, .container__investment, .container__incorporation, .container__email, .container__remote-work, .container__finance, .container__marketing, .container__legal, .container__analytics, .container__operations, .container__datascience, .container__manufacturing, .container__software-development, .container__equity, .container__3D_Printing, .container__Monitoring {
        background: color-card-inner-container-dark-mode;
        &:before {
        @extend .tooltip;
    }
        div{
           @extend .card;
        }
    }
}

   

// animate the SVG line to show the dotted line moving downward
// 5rem being the height of the SVG
@keyframes animateline {
    from {
        stroke-dashoffset: 0;
    }

    to {
        stroke-dashoffset: -5rem;
    }
}